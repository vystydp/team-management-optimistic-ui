# Backend Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real secrets!

# Server Configuration
PORT=3001
NODE_ENV=development

# Authentication Secrets
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-a-secure-random-string-in-production
SESSION_SECRET=change-this-to-a-different-secure-random-string

# GitHub OAuth Configuration
# Create OAuth App: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here
GITHUB_CALLBACK_URL=http://localhost:3001/auth/callback

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Kubernetes Configuration (optional for local dev)
KUBE_CONTEXT=kind-team-mgmt-local
CROSSPLANE_NAMESPACE=crossplane-system
APP_NAMESPACE=team-environments

# Production Checklist:
# [ ] Generate new random JWT_SECRET (64+ chars)
# [ ] Generate new random SESSION_SECRET (64+ chars)
# [ ] Create production GitHub OAuth App
# [ ] Set NODE_ENV=production
# [ ] Update FRONTEND_URL to production domain
# [ ] Update GITHUB_CALLBACK_URL to production backend
# [ ] Store secrets in secure secret manager (not in .env file)
